{
  "name": "glass-virtual-try-on",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "test": "vitest run",
    "build": "tsc && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35",
    "@vitejs/plugin-basic-ssl": "^2.3.0",
    "gh-pages": "^6.3.0",
    "three": "^0.184.0",
    "typescript": "^5.8.3",
    "vite": "^8.0.16"
  },
  "overrides": {
    "esbuild": "^0.28.0"
  },
  "devDependencies": {
    "@types/three": "^0.184.1",
    "playwright": "^1.61.0",
    "vitest": "^4.1.9"
  }
}
